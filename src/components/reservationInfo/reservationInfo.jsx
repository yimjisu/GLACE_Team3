import React, { useState, useRef, useEffect } from "react";
import styles from './reservationInfo.module.css';

const ReservationInfo = ({ 
    state, setState, showInfo
    }) => {

    return (
        <div>예약 정보 서버에서 받아와야됨</div>
    );

}

export default ReservationInfo;